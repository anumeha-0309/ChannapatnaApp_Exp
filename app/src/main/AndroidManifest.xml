<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.channapatnaappv2">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChannapatnaApp">

        <activity android:name=".HowItsMadeActivity" android:exported="false" />
        <activity android:name=".CatalogActivity" android:exported="false" />
        <activity android:name=".MeetMakerActivity" android:exported="false" />
        <activity android:name=".VerifyActivity" android:exported="false" />
        <activity android:name=".HowToDetailActivity" android:exported="false" />
        <activity android:name=".MakerProfileActivity" android:exported="false" />
        <activity android:name=".ArtistRegistrationActivity" android:exported="false" />

        <!-- ✅ ADD THIS -->
        <activity android:name=".ToyDetailActivity" />

        <!-- Launcher Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LanguageActivity"
            android:exported="false" />

        <!-- Main screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>